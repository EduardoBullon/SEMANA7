<% 
const title = '404 - Página No Encontrada';
const user = null; // May or may not have user context
%>

<%- include('partials/header', { title, user }) %>

<div class="row">
    <div class="col s12">
        <div class="error-page">
            <div class="error-code">404</div>
            <h3>
                <i class="material-icons large orange-text">search_off</i>
                Página No Encontrada
            </h3>
            <p class="grey-text">
                La página que buscas no existe o ha sido movida.
            </p>
            
            <div style="margin-top: 40px;">
                <% if (typeof user !== 'undefined' && user) { %>
                    <% if (user.roles && user.roles.includes('admin')) { %>
                        <a href="/dashboard/admin" class="btn green waves-effect waves-light">
                            <i class="material-icons left">admin_panel_settings</i>
                            Dashboard Admin
                        </a>
                    <% } else { %>
                        <a href="/dashboard/user" class="btn green waves-effect waves-light">
                            <i class="material-icons left">dashboard</i>
                            Mi Dashboard
                        </a>
                    <% } %>
                    
                    <a href="/profile" class="btn green waves-effect waves-light">
                        <i class="material-icons left">person</i>
                        Mi Perfil
                    </a>
                <% } else { %>
                    <a href="/signin" class="btn green waves-effect waves-light">
                        <i class="material-icons left">login</i>
                        Iniciar Sesión
                    </a>
                    
                    <a href="/signup" class="btn green waves-effect waves-light">
                        <i class="material-icons left">person_add</i>
                        Registrarse
                    </a>
                <% } %>
                
                <a href="/" class="btn-flat waves-effect waves-light">
                    <i class="material-icons left">home</i>
                    Ir al Inicio
                </a>
            </div>
            
            <div class="card-panel blue lighten-4 blue-text text-darken-2" style="margin-top: 30px; max-width: 600px; margin-left: auto; margin-right: auto;">
                <i class="material-icons left">lightbulb_outline</i>
                <strong>¿Qué puedes hacer?</strong>
                <ul style="margin-left: 20px; margin-top: 10px;">
                    <li>Verificar la URL en la barra de direcciones</li>
                    <li>Usar los enlaces de navegación arriba</li>
                    <li>Regresar a la página anterior usando el botón atrás del navegador</li>
                </ul>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>